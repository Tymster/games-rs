enum Tile {
    Empty,
    Cross,
    Nought,
}
struct Board {
    tiles: [[Tile; 3]; 3],
}
impl Board {
    fn new() {}
    fn canplace(x: u8, y: u8) {}
}
pub fn noughts() {
    println!("Jerome");
}
